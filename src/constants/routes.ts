const ROUTES = {
    DEFAULT: '/',
    WELCOME: '/welcome',
    DASHBOARD: '/dashboard',
    RESOURCES: '/resources',
    SETTINGS: '/settings',
    NOT_FOUND: '*',
} as const;

export default ROUTES;
